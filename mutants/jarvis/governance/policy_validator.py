# jarvis/governance/policy_validator.py
# Version: 1.0.0
# Authority: Master FAS v6.1.0-G -- Governance Integration Layer

from __future__ import annotations
from dataclasses import dataclass
from typing import List
from jarvis.core.regime import GlobalRegimeState
from jarvis.utils.constants import QUALITY_SCORE_CAP_UNDER_UNCERTAINTY

_WINDOW_MINIMUM: int                  = 20
_STEP_MINIMUM: int                    = 1
_PERIODS_PER_YEAR_MIN: int            = 1
_PERIODS_PER_YEAR_MAX: int            = 1000
_CRISIS_MIN_META_UNCERTAINTY: float   = 0.5
_STANDARD_PERIODS_PER_YEAR: frozenset = frozenset({12, 52, 252})
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


@dataclass(frozen=True)
class PolicyViolation:
    rule_id:        str
    field_name:     str
    observed_value: object
    message:        str
    is_blocking:    bool


@dataclass(frozen=True)
class PolicyValidationResult:
    is_compliant:        bool
    violations:          tuple
    warnings:            tuple
    blocking_violations: tuple
    validated_fields:    tuple


def validate_pipeline_config(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    args = [meta_uncertainty, initial_capital, window, step, regime, periods_per_year]# type: ignore
    kwargs = {}# type: ignore
    return _mutmut_trampoline(x_validate_pipeline_config__mutmut_orig, x_validate_pipeline_config__mutmut_mutants, args, kwargs, None)


def x_validate_pipeline_config__mutmut_orig(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_1(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 253,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_2(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = None
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_3(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = None

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_4(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append(None)
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_5(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("XXmeta_uncertaintyXX")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_6(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("META_UNCERTAINTY")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_7(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_8(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(None)
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_9(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation(None, "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_10(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", None, meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_11(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", None,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_12(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            None, True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_13(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", None))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_14(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_15(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_16(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_17(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_18(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", ))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_19(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("XXGOV-01XX", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_20(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("gov-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_21(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "XXmeta_uncertaintyXX", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_22(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "META_UNCERTAINTY", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_23(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(None).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_24(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", False))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_25(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_26(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (1.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_27(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 < float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_28(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(None) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_29(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) < 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_30(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 2.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_31(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(None)

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_32(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation(None, "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_33(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", None, meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_34(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", None,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_35(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            None, True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_36(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", None))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_37(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_38(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_39(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_40(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_41(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", ))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_42(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("XXGOV-01XX", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_43(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("gov-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_44(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "XXmeta_uncertaintyXX", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_45(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "META_UNCERTAINTY", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_46(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", False))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_47(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append(None)
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_48(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("XXinitial_capitalXX")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_49(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("INITIAL_CAPITAL")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_50(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_51(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(None)
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_52(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation(None, "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_53(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", None, initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_54(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", None,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_55(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            None, True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_56(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", None))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_57(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_58(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_59(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_60(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_61(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", ))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_62(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("XXGOV-02XX", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_63(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("gov-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_64(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "XXinitial_capitalXX", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_65(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "INITIAL_CAPITAL", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_66(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(None).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_67(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", False))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_68(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(None) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_69(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) < 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_70(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 1.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_71(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(None)

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_72(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation(None, "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_73(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", None, initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_74(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", None,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_75(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            None, True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_76(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", None))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_77(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_78(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_79(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_80(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_81(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", ))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_82(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("XXGOV-02XX", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_83(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("gov-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_84(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "XXinitial_capitalXX", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_85(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "INITIAL_CAPITAL", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_86(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", False))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_87(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append(None)
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_88(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("XXwindowXX")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_89(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("WINDOW")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_90(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_91(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(None)
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_92(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation(None, "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_93(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", None, window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_94(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", None,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_95(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            None, True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_96(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", None))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_97(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_98(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_99(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_100(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_101(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", ))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_102(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("XXGOV-03XX", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_103(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("gov-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_104(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "XXwindowXX", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_105(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "WINDOW", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_106(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(None).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_107(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", False))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_108(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window <= _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_109(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(None)

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_110(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation(None, "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_111(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", None, window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_112(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", None,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_113(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            None, True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_114(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", None))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_115(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_116(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_117(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_118(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_119(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", ))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_120(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("XXGOV-03XX", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_121(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("gov-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_122(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "XXwindowXX", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_123(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "WINDOW", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_124(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", False))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_125(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append(None)
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_126(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("XXstepXX")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_127(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("STEP")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_128(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_129(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(None)
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_130(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation(None, "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_131(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", None, step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_132(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", None,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_133(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            None, True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_134(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", None))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_135(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_136(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_137(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_138(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_139(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", ))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_140(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("XXGOV-04XX", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_141(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("gov-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_142(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "XXstepXX", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_143(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "STEP", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_144(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(None).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_145(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", False))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_146(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step <= _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_147(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(None)
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_148(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation(None, "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_149(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", None, step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_150(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", None,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_151(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            None, True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_152(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", None))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_153(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_154(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_155(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_156(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_157(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", ))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_158(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("XXGOV-04XX", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_159(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("gov-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_160(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "XXstepXX", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_161(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "STEP", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_162(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", False))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_163(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) or step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_164(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step >= window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_165(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(None)

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_166(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation(None, "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_167(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", None, step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_168(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", None,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_169(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            None, True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_170(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", None))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_171(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_172(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_173(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_174(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_175(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", ))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_176(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("XXGOV-04XX", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_177(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("gov-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_178(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "XXstepXX", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_179(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "STEP", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_180(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", False))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_181(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append(None)
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_182(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("XXregimeXX")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_183(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("REGIME")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_184(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_185(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(None)

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_186(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation(None, "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_187(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", None, regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_188(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", None,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_189(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            None, True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_190(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", None))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_191(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_192(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_193(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_194(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_195(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", ))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_196(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("XXGOV-05XX", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_197(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("gov-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_198(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "XXregimeXX", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_199(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "REGIME", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_200(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(None).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_201(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", False))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_202(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append(None)
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_203(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("XXregime_meta_uncertainty_coherenceXX")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_204(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("REGIME_META_UNCERTAINTY_COHERENCE")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_205(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0 or float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_206(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float)) or 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_207(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS or isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_208(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState) or regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_209(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime != GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_210(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 1.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_211(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 < float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_212(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(None) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_213(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) < 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_214(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 2.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_215(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(None) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_216(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) <= _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_217(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(None)

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_218(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation(None, "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_219(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", None, meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_220(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", None,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_221(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            None, True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_222(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", None))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_223(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_224(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_225(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_226(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_227(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", ))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_228(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("XXGOV-06XX", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_229(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("gov-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_230(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "XXmeta_uncertaintyXX", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_231(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "META_UNCERTAINTY", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_232(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", False))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_233(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append(None)
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_234(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("XXhigh_uncertainty_capital_floorXX")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_235(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("HIGH_UNCERTAINTY_CAPITAL_FLOOR")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_236(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float)) or float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_237(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY or isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_238(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0 or float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_239(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float)) or 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_240(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 1.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_241(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 < float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_242(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(None) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_243(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) < 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_244(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 2.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_245(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(None) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_246(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) > QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_247(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(None) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_248(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) < 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_249(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 1.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_250(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(None)

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_251(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation(None, "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_252(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", None, initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_253(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", None,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_254(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            None, False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_255(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", None))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_256(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_257(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_258(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_259(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_260(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", ))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_261(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("XXGOV-07XX", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_262(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("gov-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_263(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "XXinitial_capitalXX", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_264(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "INITIAL_CAPITAL", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_265(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", True))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_266(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append(None)
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_267(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("XXperiods_per_yearXX")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_268(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("PERIODS_PER_YEAR")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_269(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_270(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(None)
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_271(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation(None, "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_272(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", None, periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_273(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", None,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_274(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            None, True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_275(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", None))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_276(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_277(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_278(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_279(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_280(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", ))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_281(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("XXGOV-08XX", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_282(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("gov-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_283(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "XXperiods_per_yearXX", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_284(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "PERIODS_PER_YEAR", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_285(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(None).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_286(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", False))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_287(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_288(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN < periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_289(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year < _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_290(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(None)
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_291(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation(None, "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_292(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", None, periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_293(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", None,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_294(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            None, True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_295(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", None))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_296(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_297(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_298(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_299(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_300(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", ))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_301(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("XXGOV-08XX", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_302(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("gov-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_303(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "XXperiods_per_yearXX", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_304(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "PERIODS_PER_YEAR", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_305(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", False))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_306(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_307(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(None)

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_308(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation(None, "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_309(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", None, periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_310(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", None,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_311(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            None, False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_312(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", None))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_313(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_314(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_315(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_316(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_317(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", ))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_318(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("XXGOV-08XX", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_319(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("gov-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_320(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "XXperiods_per_yearXX", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_321(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "PERIODS_PER_YEAR", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_322(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(None)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_323(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", True))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_324(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = None
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_325(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(None)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_326(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = None
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_327(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(None)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_328(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_329(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=None,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_330(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=None,
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_331(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=None,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_332(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=None,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_333(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=None,
    )


def x_validate_pipeline_config__mutmut_334(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_335(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_336(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_337(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_338(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        )


def x_validate_pipeline_config__mutmut_339(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) != 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_340(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 1,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_341(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(None),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(validated_fields),
    )


def x_validate_pipeline_config__mutmut_342(
    meta_uncertainty: float,
    initial_capital: float,
    window: int,
    step: int,
    regime: GlobalRegimeState,
    periods_per_year: int = 252,
) -> PolicyValidationResult:
    violations: List[PolicyViolation] = []
    validated_fields: List[str] = []

    validated_fields.append("meta_uncertainty")
    if not isinstance(meta_uncertainty, (int, float)):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss numerisch sein; erhalten: {type(meta_uncertainty).__name__}.", True))
    elif not (0.0 <= float(meta_uncertainty) <= 1.0):
        violations.append(PolicyViolation("GOV-01", "meta_uncertainty", meta_uncertainty,
            f"meta_uncertainty muss in [0.0, 1.0] liegen; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("initial_capital")
    if not isinstance(initial_capital, (int, float)):
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss numerisch sein; erhalten: {type(initial_capital).__name__}.", True))
    elif float(initial_capital) <= 0.0:
        violations.append(PolicyViolation("GOV-02", "initial_capital", initial_capital,
            f"initial_capital muss strikt positiv sein (> 0.0); erhalten: {initial_capital}.", True))

    validated_fields.append("window")
    if not isinstance(window, int):
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss ein Integer sein; erhalten: {type(window).__name__}.", True))
    elif window < _WINDOW_MINIMUM:
        violations.append(PolicyViolation("GOV-03", "window", window,
            f"window muss >= {_WINDOW_MINIMUM} sein (spiegelt RiskEngine-Minimum); erhalten: {window}.", True))

    validated_fields.append("step")
    if not isinstance(step, int):
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss ein Integer sein; erhalten: {type(step).__name__}.", True))
    elif step < _STEP_MINIMUM:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss >= {_STEP_MINIMUM} sein; erhalten: {step}.", True))
    elif isinstance(window, int) and step > window:
        violations.append(PolicyViolation("GOV-04", "step", step,
            f"step muss <= window ({window}) sein; erhalten: {step}.", True))

    validated_fields.append("regime")
    if not isinstance(regime, GlobalRegimeState):
        violations.append(PolicyViolation("GOV-05", "regime", regime,
            f"regime muss eine GlobalRegimeState-Enum-Instanz sein; erhalten: {type(regime).__name__}.", True))

    validated_fields.append("regime_meta_uncertainty_coherence")
    if (isinstance(regime, GlobalRegimeState)
            and regime == GlobalRegimeState.CRISIS
            and isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) < _CRISIS_MIN_META_UNCERTAINTY):
        violations.append(PolicyViolation("GOV-06", "meta_uncertainty", meta_uncertainty,
            f"Bei regime=CRISIS muss meta_uncertainty >= {_CRISIS_MIN_META_UNCERTAINTY} sein; erhalten: {meta_uncertainty}.", True))

    validated_fields.append("high_uncertainty_capital_floor")
    if (isinstance(meta_uncertainty, (int, float))
            and 0.0 <= float(meta_uncertainty) <= 1.0
            and float(meta_uncertainty) >= QUALITY_SCORE_CAP_UNDER_UNCERTAINTY
            and isinstance(initial_capital, (int, float))
            and float(initial_capital) <= 0.0):
        violations.append(PolicyViolation("GOV-07", "initial_capital", initial_capital,
            f"Bei hoher meta_uncertainty (>= {QUALITY_SCORE_CAP_UNDER_UNCERTAINTY}) "
            f"muss initial_capital > 0.0 sein; erhalten: {initial_capital}. "
            f"(Advisory: GOV-02 flaggt dies bereits als blocking.)", False))

    validated_fields.append("periods_per_year")
    if not isinstance(periods_per_year, int):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss ein Integer sein; erhalten: {type(periods_per_year).__name__}.", True))
    elif not (_PERIODS_PER_YEAR_MIN <= periods_per_year <= _PERIODS_PER_YEAR_MAX):
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year muss in [{_PERIODS_PER_YEAR_MIN}, {_PERIODS_PER_YEAR_MAX}] liegen; erhalten: {periods_per_year}.", True))
    elif periods_per_year not in _STANDARD_PERIODS_PER_YEAR:
        violations.append(PolicyViolation("GOV-08", "periods_per_year", periods_per_year,
            f"periods_per_year={periods_per_year} ist nicht-standard. "
            f"Standard-Werte: {sorted(_STANDARD_PERIODS_PER_YEAR)}. Wert akzeptiert, aber zur Audit-Pruefung markiert.", False))

    blocking = tuple(v for v in violations if v.is_blocking)
    advisory = tuple(v for v in violations if not v.is_blocking)
    return PolicyValidationResult(
        is_compliant=len(blocking) == 0,
        violations=tuple(violations),
        warnings=advisory,
        blocking_violations=blocking,
        validated_fields=tuple(None),
    )

x_validate_pipeline_config__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
'x_validate_pipeline_config__mutmut_1': x_validate_pipeline_config__mutmut_1, 
    'x_validate_pipeline_config__mutmut_2': x_validate_pipeline_config__mutmut_2, 
    'x_validate_pipeline_config__mutmut_3': x_validate_pipeline_config__mutmut_3, 
    'x_validate_pipeline_config__mutmut_4': x_validate_pipeline_config__mutmut_4, 
    'x_validate_pipeline_config__mutmut_5': x_validate_pipeline_config__mutmut_5, 
    'x_validate_pipeline_config__mutmut_6': x_validate_pipeline_config__mutmut_6, 
    'x_validate_pipeline_config__mutmut_7': x_validate_pipeline_config__mutmut_7, 
    'x_validate_pipeline_config__mutmut_8': x_validate_pipeline_config__mutmut_8, 
    'x_validate_pipeline_config__mutmut_9': x_validate_pipeline_config__mutmut_9, 
    'x_validate_pipeline_config__mutmut_10': x_validate_pipeline_config__mutmut_10, 
    'x_validate_pipeline_config__mutmut_11': x_validate_pipeline_config__mutmut_11, 
    'x_validate_pipeline_config__mutmut_12': x_validate_pipeline_config__mutmut_12, 
    'x_validate_pipeline_config__mutmut_13': x_validate_pipeline_config__mutmut_13, 
    'x_validate_pipeline_config__mutmut_14': x_validate_pipeline_config__mutmut_14, 
    'x_validate_pipeline_config__mutmut_15': x_validate_pipeline_config__mutmut_15, 
    'x_validate_pipeline_config__mutmut_16': x_validate_pipeline_config__mutmut_16, 
    'x_validate_pipeline_config__mutmut_17': x_validate_pipeline_config__mutmut_17, 
    'x_validate_pipeline_config__mutmut_18': x_validate_pipeline_config__mutmut_18, 
    'x_validate_pipeline_config__mutmut_19': x_validate_pipeline_config__mutmut_19, 
    'x_validate_pipeline_config__mutmut_20': x_validate_pipeline_config__mutmut_20, 
    'x_validate_pipeline_config__mutmut_21': x_validate_pipeline_config__mutmut_21, 
    'x_validate_pipeline_config__mutmut_22': x_validate_pipeline_config__mutmut_22, 
    'x_validate_pipeline_config__mutmut_23': x_validate_pipeline_config__mutmut_23, 
    'x_validate_pipeline_config__mutmut_24': x_validate_pipeline_config__mutmut_24, 
    'x_validate_pipeline_config__mutmut_25': x_validate_pipeline_config__mutmut_25, 
    'x_validate_pipeline_config__mutmut_26': x_validate_pipeline_config__mutmut_26, 
    'x_validate_pipeline_config__mutmut_27': x_validate_pipeline_config__mutmut_27, 
    'x_validate_pipeline_config__mutmut_28': x_validate_pipeline_config__mutmut_28, 
    'x_validate_pipeline_config__mutmut_29': x_validate_pipeline_config__mutmut_29, 
    'x_validate_pipeline_config__mutmut_30': x_validate_pipeline_config__mutmut_30, 
    'x_validate_pipeline_config__mutmut_31': x_validate_pipeline_config__mutmut_31, 
    'x_validate_pipeline_config__mutmut_32': x_validate_pipeline_config__mutmut_32, 
    'x_validate_pipeline_config__mutmut_33': x_validate_pipeline_config__mutmut_33, 
    'x_validate_pipeline_config__mutmut_34': x_validate_pipeline_config__mutmut_34, 
    'x_validate_pipeline_config__mutmut_35': x_validate_pipeline_config__mutmut_35, 
    'x_validate_pipeline_config__mutmut_36': x_validate_pipeline_config__mutmut_36, 
    'x_validate_pipeline_config__mutmut_37': x_validate_pipeline_config__mutmut_37, 
    'x_validate_pipeline_config__mutmut_38': x_validate_pipeline_config__mutmut_38, 
    'x_validate_pipeline_config__mutmut_39': x_validate_pipeline_config__mutmut_39, 
    'x_validate_pipeline_config__mutmut_40': x_validate_pipeline_config__mutmut_40, 
    'x_validate_pipeline_config__mutmut_41': x_validate_pipeline_config__mutmut_41, 
    'x_validate_pipeline_config__mutmut_42': x_validate_pipeline_config__mutmut_42, 
    'x_validate_pipeline_config__mutmut_43': x_validate_pipeline_config__mutmut_43, 
    'x_validate_pipeline_config__mutmut_44': x_validate_pipeline_config__mutmut_44, 
    'x_validate_pipeline_config__mutmut_45': x_validate_pipeline_config__mutmut_45, 
    'x_validate_pipeline_config__mutmut_46': x_validate_pipeline_config__mutmut_46, 
    'x_validate_pipeline_config__mutmut_47': x_validate_pipeline_config__mutmut_47, 
    'x_validate_pipeline_config__mutmut_48': x_validate_pipeline_config__mutmut_48, 
    'x_validate_pipeline_config__mutmut_49': x_validate_pipeline_config__mutmut_49, 
    'x_validate_pipeline_config__mutmut_50': x_validate_pipeline_config__mutmut_50, 
    'x_validate_pipeline_config__mutmut_51': x_validate_pipeline_config__mutmut_51, 
    'x_validate_pipeline_config__mutmut_52': x_validate_pipeline_config__mutmut_52, 
    'x_validate_pipeline_config__mutmut_53': x_validate_pipeline_config__mutmut_53, 
    'x_validate_pipeline_config__mutmut_54': x_validate_pipeline_config__mutmut_54, 
    'x_validate_pipeline_config__mutmut_55': x_validate_pipeline_config__mutmut_55, 
    'x_validate_pipeline_config__mutmut_56': x_validate_pipeline_config__mutmut_56, 
    'x_validate_pipeline_config__mutmut_57': x_validate_pipeline_config__mutmut_57, 
    'x_validate_pipeline_config__mutmut_58': x_validate_pipeline_config__mutmut_58, 
    'x_validate_pipeline_config__mutmut_59': x_validate_pipeline_config__mutmut_59, 
    'x_validate_pipeline_config__mutmut_60': x_validate_pipeline_config__mutmut_60, 
    'x_validate_pipeline_config__mutmut_61': x_validate_pipeline_config__mutmut_61, 
    'x_validate_pipeline_config__mutmut_62': x_validate_pipeline_config__mutmut_62, 
    'x_validate_pipeline_config__mutmut_63': x_validate_pipeline_config__mutmut_63, 
    'x_validate_pipeline_config__mutmut_64': x_validate_pipeline_config__mutmut_64, 
    'x_validate_pipeline_config__mutmut_65': x_validate_pipeline_config__mutmut_65, 
    'x_validate_pipeline_config__mutmut_66': x_validate_pipeline_config__mutmut_66, 
    'x_validate_pipeline_config__mutmut_67': x_validate_pipeline_config__mutmut_67, 
    'x_validate_pipeline_config__mutmut_68': x_validate_pipeline_config__mutmut_68, 
    'x_validate_pipeline_config__mutmut_69': x_validate_pipeline_config__mutmut_69, 
    'x_validate_pipeline_config__mutmut_70': x_validate_pipeline_config__mutmut_70, 
    'x_validate_pipeline_config__mutmut_71': x_validate_pipeline_config__mutmut_71, 
    'x_validate_pipeline_config__mutmut_72': x_validate_pipeline_config__mutmut_72, 
    'x_validate_pipeline_config__mutmut_73': x_validate_pipeline_config__mutmut_73, 
    'x_validate_pipeline_config__mutmut_74': x_validate_pipeline_config__mutmut_74, 
    'x_validate_pipeline_config__mutmut_75': x_validate_pipeline_config__mutmut_75, 
    'x_validate_pipeline_config__mutmut_76': x_validate_pipeline_config__mutmut_76, 
    'x_validate_pipeline_config__mutmut_77': x_validate_pipeline_config__mutmut_77, 
    'x_validate_pipeline_config__mutmut_78': x_validate_pipeline_config__mutmut_78, 
    'x_validate_pipeline_config__mutmut_79': x_validate_pipeline_config__mutmut_79, 
    'x_validate_pipeline_config__mutmut_80': x_validate_pipeline_config__mutmut_80, 
    'x_validate_pipeline_config__mutmut_81': x_validate_pipeline_config__mutmut_81, 
    'x_validate_pipeline_config__mutmut_82': x_validate_pipeline_config__mutmut_82, 
    'x_validate_pipeline_config__mutmut_83': x_validate_pipeline_config__mutmut_83, 
    'x_validate_pipeline_config__mutmut_84': x_validate_pipeline_config__mutmut_84, 
    'x_validate_pipeline_config__mutmut_85': x_validate_pipeline_config__mutmut_85, 
    'x_validate_pipeline_config__mutmut_86': x_validate_pipeline_config__mutmut_86, 
    'x_validate_pipeline_config__mutmut_87': x_validate_pipeline_config__mutmut_87, 
    'x_validate_pipeline_config__mutmut_88': x_validate_pipeline_config__mutmut_88, 
    'x_validate_pipeline_config__mutmut_89': x_validate_pipeline_config__mutmut_89, 
    'x_validate_pipeline_config__mutmut_90': x_validate_pipeline_config__mutmut_90, 
    'x_validate_pipeline_config__mutmut_91': x_validate_pipeline_config__mutmut_91, 
    'x_validate_pipeline_config__mutmut_92': x_validate_pipeline_config__mutmut_92, 
    'x_validate_pipeline_config__mutmut_93': x_validate_pipeline_config__mutmut_93, 
    'x_validate_pipeline_config__mutmut_94': x_validate_pipeline_config__mutmut_94, 
    'x_validate_pipeline_config__mutmut_95': x_validate_pipeline_config__mutmut_95, 
    'x_validate_pipeline_config__mutmut_96': x_validate_pipeline_config__mutmut_96, 
    'x_validate_pipeline_config__mutmut_97': x_validate_pipeline_config__mutmut_97, 
    'x_validate_pipeline_config__mutmut_98': x_validate_pipeline_config__mutmut_98, 
    'x_validate_pipeline_config__mutmut_99': x_validate_pipeline_config__mutmut_99, 
    'x_validate_pipeline_config__mutmut_100': x_validate_pipeline_config__mutmut_100, 
    'x_validate_pipeline_config__mutmut_101': x_validate_pipeline_config__mutmut_101, 
    'x_validate_pipeline_config__mutmut_102': x_validate_pipeline_config__mutmut_102, 
    'x_validate_pipeline_config__mutmut_103': x_validate_pipeline_config__mutmut_103, 
    'x_validate_pipeline_config__mutmut_104': x_validate_pipeline_config__mutmut_104, 
    'x_validate_pipeline_config__mutmut_105': x_validate_pipeline_config__mutmut_105, 
    'x_validate_pipeline_config__mutmut_106': x_validate_pipeline_config__mutmut_106, 
    'x_validate_pipeline_config__mutmut_107': x_validate_pipeline_config__mutmut_107, 
    'x_validate_pipeline_config__mutmut_108': x_validate_pipeline_config__mutmut_108, 
    'x_validate_pipeline_config__mutmut_109': x_validate_pipeline_config__mutmut_109, 
    'x_validate_pipeline_config__mutmut_110': x_validate_pipeline_config__mutmut_110, 
    'x_validate_pipeline_config__mutmut_111': x_validate_pipeline_config__mutmut_111, 
    'x_validate_pipeline_config__mutmut_112': x_validate_pipeline_config__mutmut_112, 
    'x_validate_pipeline_config__mutmut_113': x_validate_pipeline_config__mutmut_113, 
    'x_validate_pipeline_config__mutmut_114': x_validate_pipeline_config__mutmut_114, 
    'x_validate_pipeline_config__mutmut_115': x_validate_pipeline_config__mutmut_115, 
    'x_validate_pipeline_config__mutmut_116': x_validate_pipeline_config__mutmut_116, 
    'x_validate_pipeline_config__mutmut_117': x_validate_pipeline_config__mutmut_117, 
    'x_validate_pipeline_config__mutmut_118': x_validate_pipeline_config__mutmut_118, 
    'x_validate_pipeline_config__mutmut_119': x_validate_pipeline_config__mutmut_119, 
    'x_validate_pipeline_config__mutmut_120': x_validate_pipeline_config__mutmut_120, 
    'x_validate_pipeline_config__mutmut_121': x_validate_pipeline_config__mutmut_121, 
    'x_validate_pipeline_config__mutmut_122': x_validate_pipeline_config__mutmut_122, 
    'x_validate_pipeline_config__mutmut_123': x_validate_pipeline_config__mutmut_123, 
    'x_validate_pipeline_config__mutmut_124': x_validate_pipeline_config__mutmut_124, 
    'x_validate_pipeline_config__mutmut_125': x_validate_pipeline_config__mutmut_125, 
    'x_validate_pipeline_config__mutmut_126': x_validate_pipeline_config__mutmut_126, 
    'x_validate_pipeline_config__mutmut_127': x_validate_pipeline_config__mutmut_127, 
    'x_validate_pipeline_config__mutmut_128': x_validate_pipeline_config__mutmut_128, 
    'x_validate_pipeline_config__mutmut_129': x_validate_pipeline_config__mutmut_129, 
    'x_validate_pipeline_config__mutmut_130': x_validate_pipeline_config__mutmut_130, 
    'x_validate_pipeline_config__mutmut_131': x_validate_pipeline_config__mutmut_131, 
    'x_validate_pipeline_config__mutmut_132': x_validate_pipeline_config__mutmut_132, 
    'x_validate_pipeline_config__mutmut_133': x_validate_pipeline_config__mutmut_133, 
    'x_validate_pipeline_config__mutmut_134': x_validate_pipeline_config__mutmut_134, 
    'x_validate_pipeline_config__mutmut_135': x_validate_pipeline_config__mutmut_135, 
    'x_validate_pipeline_config__mutmut_136': x_validate_pipeline_config__mutmut_136, 
    'x_validate_pipeline_config__mutmut_137': x_validate_pipeline_config__mutmut_137, 
    'x_validate_pipeline_config__mutmut_138': x_validate_pipeline_config__mutmut_138, 
    'x_validate_pipeline_config__mutmut_139': x_validate_pipeline_config__mutmut_139, 
    'x_validate_pipeline_config__mutmut_140': x_validate_pipeline_config__mutmut_140, 
    'x_validate_pipeline_config__mutmut_141': x_validate_pipeline_config__mutmut_141, 
    'x_validate_pipeline_config__mutmut_142': x_validate_pipeline_config__mutmut_142, 
    'x_validate_pipeline_config__mutmut_143': x_validate_pipeline_config__mutmut_143, 
    'x_validate_pipeline_config__mutmut_144': x_validate_pipeline_config__mutmut_144, 
    'x_validate_pipeline_config__mutmut_145': x_validate_pipeline_config__mutmut_145, 
    'x_validate_pipeline_config__mutmut_146': x_validate_pipeline_config__mutmut_146, 
    'x_validate_pipeline_config__mutmut_147': x_validate_pipeline_config__mutmut_147, 
    'x_validate_pipeline_config__mutmut_148': x_validate_pipeline_config__mutmut_148, 
    'x_validate_pipeline_config__mutmut_149': x_validate_pipeline_config__mutmut_149, 
    'x_validate_pipeline_config__mutmut_150': x_validate_pipeline_config__mutmut_150, 
    'x_validate_pipeline_config__mutmut_151': x_validate_pipeline_config__mutmut_151, 
    'x_validate_pipeline_config__mutmut_152': x_validate_pipeline_config__mutmut_152, 
    'x_validate_pipeline_config__mutmut_153': x_validate_pipeline_config__mutmut_153, 
    'x_validate_pipeline_config__mutmut_154': x_validate_pipeline_config__mutmut_154, 
    'x_validate_pipeline_config__mutmut_155': x_validate_pipeline_config__mutmut_155, 
    'x_validate_pipeline_config__mutmut_156': x_validate_pipeline_config__mutmut_156, 
    'x_validate_pipeline_config__mutmut_157': x_validate_pipeline_config__mutmut_157, 
    'x_validate_pipeline_config__mutmut_158': x_validate_pipeline_config__mutmut_158, 
    'x_validate_pipeline_config__mutmut_159': x_validate_pipeline_config__mutmut_159, 
    'x_validate_pipeline_config__mutmut_160': x_validate_pipeline_config__mutmut_160, 
    'x_validate_pipeline_config__mutmut_161': x_validate_pipeline_config__mutmut_161, 
    'x_validate_pipeline_config__mutmut_162': x_validate_pipeline_config__mutmut_162, 
    'x_validate_pipeline_config__mutmut_163': x_validate_pipeline_config__mutmut_163, 
    'x_validate_pipeline_config__mutmut_164': x_validate_pipeline_config__mutmut_164, 
    'x_validate_pipeline_config__mutmut_165': x_validate_pipeline_config__mutmut_165, 
    'x_validate_pipeline_config__mutmut_166': x_validate_pipeline_config__mutmut_166, 
    'x_validate_pipeline_config__mutmut_167': x_validate_pipeline_config__mutmut_167, 
    'x_validate_pipeline_config__mutmut_168': x_validate_pipeline_config__mutmut_168, 
    'x_validate_pipeline_config__mutmut_169': x_validate_pipeline_config__mutmut_169, 
    'x_validate_pipeline_config__mutmut_170': x_validate_pipeline_config__mutmut_170, 
    'x_validate_pipeline_config__mutmut_171': x_validate_pipeline_config__mutmut_171, 
    'x_validate_pipeline_config__mutmut_172': x_validate_pipeline_config__mutmut_172, 
    'x_validate_pipeline_config__mutmut_173': x_validate_pipeline_config__mutmut_173, 
    'x_validate_pipeline_config__mutmut_174': x_validate_pipeline_config__mutmut_174, 
    'x_validate_pipeline_config__mutmut_175': x_validate_pipeline_config__mutmut_175, 
    'x_validate_pipeline_config__mutmut_176': x_validate_pipeline_config__mutmut_176, 
    'x_validate_pipeline_config__mutmut_177': x_validate_pipeline_config__mutmut_177, 
    'x_validate_pipeline_config__mutmut_178': x_validate_pipeline_config__mutmut_178, 
    'x_validate_pipeline_config__mutmut_179': x_validate_pipeline_config__mutmut_179, 
    'x_validate_pipeline_config__mutmut_180': x_validate_pipeline_config__mutmut_180, 
    'x_validate_pipeline_config__mutmut_181': x_validate_pipeline_config__mutmut_181, 
    'x_validate_pipeline_config__mutmut_182': x_validate_pipeline_config__mutmut_182, 
    'x_validate_pipeline_config__mutmut_183': x_validate_pipeline_config__mutmut_183, 
    'x_validate_pipeline_config__mutmut_184': x_validate_pipeline_config__mutmut_184, 
    'x_validate_pipeline_config__mutmut_185': x_validate_pipeline_config__mutmut_185, 
    'x_validate_pipeline_config__mutmut_186': x_validate_pipeline_config__mutmut_186, 
    'x_validate_pipeline_config__mutmut_187': x_validate_pipeline_config__mutmut_187, 
    'x_validate_pipeline_config__mutmut_188': x_validate_pipeline_config__mutmut_188, 
    'x_validate_pipeline_config__mutmut_189': x_validate_pipeline_config__mutmut_189, 
    'x_validate_pipeline_config__mutmut_190': x_validate_pipeline_config__mutmut_190, 
    'x_validate_pipeline_config__mutmut_191': x_validate_pipeline_config__mutmut_191, 
    'x_validate_pipeline_config__mutmut_192': x_validate_pipeline_config__mutmut_192, 
    'x_validate_pipeline_config__mutmut_193': x_validate_pipeline_config__mutmut_193, 
    'x_validate_pipeline_config__mutmut_194': x_validate_pipeline_config__mutmut_194, 
    'x_validate_pipeline_config__mutmut_195': x_validate_pipeline_config__mutmut_195, 
    'x_validate_pipeline_config__mutmut_196': x_validate_pipeline_config__mutmut_196, 
    'x_validate_pipeline_config__mutmut_197': x_validate_pipeline_config__mutmut_197, 
    'x_validate_pipeline_config__mutmut_198': x_validate_pipeline_config__mutmut_198, 
    'x_validate_pipeline_config__mutmut_199': x_validate_pipeline_config__mutmut_199, 
    'x_validate_pipeline_config__mutmut_200': x_validate_pipeline_config__mutmut_200, 
    'x_validate_pipeline_config__mutmut_201': x_validate_pipeline_config__mutmut_201, 
    'x_validate_pipeline_config__mutmut_202': x_validate_pipeline_config__mutmut_202, 
    'x_validate_pipeline_config__mutmut_203': x_validate_pipeline_config__mutmut_203, 
    'x_validate_pipeline_config__mutmut_204': x_validate_pipeline_config__mutmut_204, 
    'x_validate_pipeline_config__mutmut_205': x_validate_pipeline_config__mutmut_205, 
    'x_validate_pipeline_config__mutmut_206': x_validate_pipeline_config__mutmut_206, 
    'x_validate_pipeline_config__mutmut_207': x_validate_pipeline_config__mutmut_207, 
    'x_validate_pipeline_config__mutmut_208': x_validate_pipeline_config__mutmut_208, 
    'x_validate_pipeline_config__mutmut_209': x_validate_pipeline_config__mutmut_209, 
    'x_validate_pipeline_config__mutmut_210': x_validate_pipeline_config__mutmut_210, 
    'x_validate_pipeline_config__mutmut_211': x_validate_pipeline_config__mutmut_211, 
    'x_validate_pipeline_config__mutmut_212': x_validate_pipeline_config__mutmut_212, 
    'x_validate_pipeline_config__mutmut_213': x_validate_pipeline_config__mutmut_213, 
    'x_validate_pipeline_config__mutmut_214': x_validate_pipeline_config__mutmut_214, 
    'x_validate_pipeline_config__mutmut_215': x_validate_pipeline_config__mutmut_215, 
    'x_validate_pipeline_config__mutmut_216': x_validate_pipeline_config__mutmut_216, 
    'x_validate_pipeline_config__mutmut_217': x_validate_pipeline_config__mutmut_217, 
    'x_validate_pipeline_config__mutmut_218': x_validate_pipeline_config__mutmut_218, 
    'x_validate_pipeline_config__mutmut_219': x_validate_pipeline_config__mutmut_219, 
    'x_validate_pipeline_config__mutmut_220': x_validate_pipeline_config__mutmut_220, 
    'x_validate_pipeline_config__mutmut_221': x_validate_pipeline_config__mutmut_221, 
    'x_validate_pipeline_config__mutmut_222': x_validate_pipeline_config__mutmut_222, 
    'x_validate_pipeline_config__mutmut_223': x_validate_pipeline_config__mutmut_223, 
    'x_validate_pipeline_config__mutmut_224': x_validate_pipeline_config__mutmut_224, 
    'x_validate_pipeline_config__mutmut_225': x_validate_pipeline_config__mutmut_225, 
    'x_validate_pipeline_config__mutmut_226': x_validate_pipeline_config__mutmut_226, 
    'x_validate_pipeline_config__mutmut_227': x_validate_pipeline_config__mutmut_227, 
    'x_validate_pipeline_config__mutmut_228': x_validate_pipeline_config__mutmut_228, 
    'x_validate_pipeline_config__mutmut_229': x_validate_pipeline_config__mutmut_229, 
    'x_validate_pipeline_config__mutmut_230': x_validate_pipeline_config__mutmut_230, 
    'x_validate_pipeline_config__mutmut_231': x_validate_pipeline_config__mutmut_231, 
    'x_validate_pipeline_config__mutmut_232': x_validate_pipeline_config__mutmut_232, 
    'x_validate_pipeline_config__mutmut_233': x_validate_pipeline_config__mutmut_233, 
    'x_validate_pipeline_config__mutmut_234': x_validate_pipeline_config__mutmut_234, 
    'x_validate_pipeline_config__mutmut_235': x_validate_pipeline_config__mutmut_235, 
    'x_validate_pipeline_config__mutmut_236': x_validate_pipeline_config__mutmut_236, 
    'x_validate_pipeline_config__mutmut_237': x_validate_pipeline_config__mutmut_237, 
    'x_validate_pipeline_config__mutmut_238': x_validate_pipeline_config__mutmut_238, 
    'x_validate_pipeline_config__mutmut_239': x_validate_pipeline_config__mutmut_239, 
    'x_validate_pipeline_config__mutmut_240': x_validate_pipeline_config__mutmut_240, 
    'x_validate_pipeline_config__mutmut_241': x_validate_pipeline_config__mutmut_241, 
    'x_validate_pipeline_config__mutmut_242': x_validate_pipeline_config__mutmut_242, 
    'x_validate_pipeline_config__mutmut_243': x_validate_pipeline_config__mutmut_243, 
    'x_validate_pipeline_config__mutmut_244': x_validate_pipeline_config__mutmut_244, 
    'x_validate_pipeline_config__mutmut_245': x_validate_pipeline_config__mutmut_245, 
    'x_validate_pipeline_config__mutmut_246': x_validate_pipeline_config__mutmut_246, 
    'x_validate_pipeline_config__mutmut_247': x_validate_pipeline_config__mutmut_247, 
    'x_validate_pipeline_config__mutmut_248': x_validate_pipeline_config__mutmut_248, 
    'x_validate_pipeline_config__mutmut_249': x_validate_pipeline_config__mutmut_249, 
    'x_validate_pipeline_config__mutmut_250': x_validate_pipeline_config__mutmut_250, 
    'x_validate_pipeline_config__mutmut_251': x_validate_pipeline_config__mutmut_251, 
    'x_validate_pipeline_config__mutmut_252': x_validate_pipeline_config__mutmut_252, 
    'x_validate_pipeline_config__mutmut_253': x_validate_pipeline_config__mutmut_253, 
    'x_validate_pipeline_config__mutmut_254': x_validate_pipeline_config__mutmut_254, 
    'x_validate_pipeline_config__mutmut_255': x_validate_pipeline_config__mutmut_255, 
    'x_validate_pipeline_config__mutmut_256': x_validate_pipeline_config__mutmut_256, 
    'x_validate_pipeline_config__mutmut_257': x_validate_pipeline_config__mutmut_257, 
    'x_validate_pipeline_config__mutmut_258': x_validate_pipeline_config__mutmut_258, 
    'x_validate_pipeline_config__mutmut_259': x_validate_pipeline_config__mutmut_259, 
    'x_validate_pipeline_config__mutmut_260': x_validate_pipeline_config__mutmut_260, 
    'x_validate_pipeline_config__mutmut_261': x_validate_pipeline_config__mutmut_261, 
    'x_validate_pipeline_config__mutmut_262': x_validate_pipeline_config__mutmut_262, 
    'x_validate_pipeline_config__mutmut_263': x_validate_pipeline_config__mutmut_263, 
    'x_validate_pipeline_config__mutmut_264': x_validate_pipeline_config__mutmut_264, 
    'x_validate_pipeline_config__mutmut_265': x_validate_pipeline_config__mutmut_265, 
    'x_validate_pipeline_config__mutmut_266': x_validate_pipeline_config__mutmut_266, 
    'x_validate_pipeline_config__mutmut_267': x_validate_pipeline_config__mutmut_267, 
    'x_validate_pipeline_config__mutmut_268': x_validate_pipeline_config__mutmut_268, 
    'x_validate_pipeline_config__mutmut_269': x_validate_pipeline_config__mutmut_269, 
    'x_validate_pipeline_config__mutmut_270': x_validate_pipeline_config__mutmut_270, 
    'x_validate_pipeline_config__mutmut_271': x_validate_pipeline_config__mutmut_271, 
    'x_validate_pipeline_config__mutmut_272': x_validate_pipeline_config__mutmut_272, 
    'x_validate_pipeline_config__mutmut_273': x_validate_pipeline_config__mutmut_273, 
    'x_validate_pipeline_config__mutmut_274': x_validate_pipeline_config__mutmut_274, 
    'x_validate_pipeline_config__mutmut_275': x_validate_pipeline_config__mutmut_275, 
    'x_validate_pipeline_config__mutmut_276': x_validate_pipeline_config__mutmut_276, 
    'x_validate_pipeline_config__mutmut_277': x_validate_pipeline_config__mutmut_277, 
    'x_validate_pipeline_config__mutmut_278': x_validate_pipeline_config__mutmut_278, 
    'x_validate_pipeline_config__mutmut_279': x_validate_pipeline_config__mutmut_279, 
    'x_validate_pipeline_config__mutmut_280': x_validate_pipeline_config__mutmut_280, 
    'x_validate_pipeline_config__mutmut_281': x_validate_pipeline_config__mutmut_281, 
    'x_validate_pipeline_config__mutmut_282': x_validate_pipeline_config__mutmut_282, 
    'x_validate_pipeline_config__mutmut_283': x_validate_pipeline_config__mutmut_283, 
    'x_validate_pipeline_config__mutmut_284': x_validate_pipeline_config__mutmut_284, 
    'x_validate_pipeline_config__mutmut_285': x_validate_pipeline_config__mutmut_285, 
    'x_validate_pipeline_config__mutmut_286': x_validate_pipeline_config__mutmut_286, 
    'x_validate_pipeline_config__mutmut_287': x_validate_pipeline_config__mutmut_287, 
    'x_validate_pipeline_config__mutmut_288': x_validate_pipeline_config__mutmut_288, 
    'x_validate_pipeline_config__mutmut_289': x_validate_pipeline_config__mutmut_289, 
    'x_validate_pipeline_config__mutmut_290': x_validate_pipeline_config__mutmut_290, 
    'x_validate_pipeline_config__mutmut_291': x_validate_pipeline_config__mutmut_291, 
    'x_validate_pipeline_config__mutmut_292': x_validate_pipeline_config__mutmut_292, 
    'x_validate_pipeline_config__mutmut_293': x_validate_pipeline_config__mutmut_293, 
    'x_validate_pipeline_config__mutmut_294': x_validate_pipeline_config__mutmut_294, 
    'x_validate_pipeline_config__mutmut_295': x_validate_pipeline_config__mutmut_295, 
    'x_validate_pipeline_config__mutmut_296': x_validate_pipeline_config__mutmut_296, 
    'x_validate_pipeline_config__mutmut_297': x_validate_pipeline_config__mutmut_297, 
    'x_validate_pipeline_config__mutmut_298': x_validate_pipeline_config__mutmut_298, 
    'x_validate_pipeline_config__mutmut_299': x_validate_pipeline_config__mutmut_299, 
    'x_validate_pipeline_config__mutmut_300': x_validate_pipeline_config__mutmut_300, 
    'x_validate_pipeline_config__mutmut_301': x_validate_pipeline_config__mutmut_301, 
    'x_validate_pipeline_config__mutmut_302': x_validate_pipeline_config__mutmut_302, 
    'x_validate_pipeline_config__mutmut_303': x_validate_pipeline_config__mutmut_303, 
    'x_validate_pipeline_config__mutmut_304': x_validate_pipeline_config__mutmut_304, 
    'x_validate_pipeline_config__mutmut_305': x_validate_pipeline_config__mutmut_305, 
    'x_validate_pipeline_config__mutmut_306': x_validate_pipeline_config__mutmut_306, 
    'x_validate_pipeline_config__mutmut_307': x_validate_pipeline_config__mutmut_307, 
    'x_validate_pipeline_config__mutmut_308': x_validate_pipeline_config__mutmut_308, 
    'x_validate_pipeline_config__mutmut_309': x_validate_pipeline_config__mutmut_309, 
    'x_validate_pipeline_config__mutmut_310': x_validate_pipeline_config__mutmut_310, 
    'x_validate_pipeline_config__mutmut_311': x_validate_pipeline_config__mutmut_311, 
    'x_validate_pipeline_config__mutmut_312': x_validate_pipeline_config__mutmut_312, 
    'x_validate_pipeline_config__mutmut_313': x_validate_pipeline_config__mutmut_313, 
    'x_validate_pipeline_config__mutmut_314': x_validate_pipeline_config__mutmut_314, 
    'x_validate_pipeline_config__mutmut_315': x_validate_pipeline_config__mutmut_315, 
    'x_validate_pipeline_config__mutmut_316': x_validate_pipeline_config__mutmut_316, 
    'x_validate_pipeline_config__mutmut_317': x_validate_pipeline_config__mutmut_317, 
    'x_validate_pipeline_config__mutmut_318': x_validate_pipeline_config__mutmut_318, 
    'x_validate_pipeline_config__mutmut_319': x_validate_pipeline_config__mutmut_319, 
    'x_validate_pipeline_config__mutmut_320': x_validate_pipeline_config__mutmut_320, 
    'x_validate_pipeline_config__mutmut_321': x_validate_pipeline_config__mutmut_321, 
    'x_validate_pipeline_config__mutmut_322': x_validate_pipeline_config__mutmut_322, 
    'x_validate_pipeline_config__mutmut_323': x_validate_pipeline_config__mutmut_323, 
    'x_validate_pipeline_config__mutmut_324': x_validate_pipeline_config__mutmut_324, 
    'x_validate_pipeline_config__mutmut_325': x_validate_pipeline_config__mutmut_325, 
    'x_validate_pipeline_config__mutmut_326': x_validate_pipeline_config__mutmut_326, 
    'x_validate_pipeline_config__mutmut_327': x_validate_pipeline_config__mutmut_327, 
    'x_validate_pipeline_config__mutmut_328': x_validate_pipeline_config__mutmut_328, 
    'x_validate_pipeline_config__mutmut_329': x_validate_pipeline_config__mutmut_329, 
    'x_validate_pipeline_config__mutmut_330': x_validate_pipeline_config__mutmut_330, 
    'x_validate_pipeline_config__mutmut_331': x_validate_pipeline_config__mutmut_331, 
    'x_validate_pipeline_config__mutmut_332': x_validate_pipeline_config__mutmut_332, 
    'x_validate_pipeline_config__mutmut_333': x_validate_pipeline_config__mutmut_333, 
    'x_validate_pipeline_config__mutmut_334': x_validate_pipeline_config__mutmut_334, 
    'x_validate_pipeline_config__mutmut_335': x_validate_pipeline_config__mutmut_335, 
    'x_validate_pipeline_config__mutmut_336': x_validate_pipeline_config__mutmut_336, 
    'x_validate_pipeline_config__mutmut_337': x_validate_pipeline_config__mutmut_337, 
    'x_validate_pipeline_config__mutmut_338': x_validate_pipeline_config__mutmut_338, 
    'x_validate_pipeline_config__mutmut_339': x_validate_pipeline_config__mutmut_339, 
    'x_validate_pipeline_config__mutmut_340': x_validate_pipeline_config__mutmut_340, 
    'x_validate_pipeline_config__mutmut_341': x_validate_pipeline_config__mutmut_341, 
    'x_validate_pipeline_config__mutmut_342': x_validate_pipeline_config__mutmut_342
}
x_validate_pipeline_config__mutmut_orig.__name__ = 'x_validate_pipeline_config'
